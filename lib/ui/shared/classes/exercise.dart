class Exercise {
  String name;
  String description;
  int isTimed; // might switch to bool later
  int isWeighted; // might swtich to bool later

  Exercise(this.name, this.isTimed, this.isWeighted, this.description);
}
